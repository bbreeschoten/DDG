<%= render 'refinery/admin/search_header', :url => refinery.activities_admin_activities_path %>
<div class='pagination_container'>
  <% if @activities.any? %>
    <%= render 'activities' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
